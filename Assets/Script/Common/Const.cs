using UnityEngine;

public partial class Const {
    public class SaveKeys {
        public static readonly string
            Settings = "Settings",
            LastResult = "LastResult";
    }

    public enum View {
        Title,
        Game,
        Result,
        Option
    }
}